<!DOCTYPE html>
<html>
<head>
  <title><%=(!yield(:title).blank? && yield(:title)) || "Whys | Questions & Answers" %></title>
  <% if controller_name == 'main' %>
  <meta name="keywords" content="Whys | Questions & Answers" />
  <meta name="description" content="Whys | Questions & Answerss" />
  <% end %>
  <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
  <meta name="robots" content="all" />
  <meta name="author" content="Whys" />
  <%= stylesheet_link_tag 'application' %>
  <%= javascript_include_tag 'application' %>
  <%= render 'layouts/tagit'  if (['thrs'].include? controller_name) && (["new","create","edit","update"].include? action_name) %>
  <!--[if gte IE 5.5000]>
    <%#= javascript_include_tag 'pngfixe.js' %>
  <![endif]-->
  <%= csrf_meta_tag %>

</head>
<body>
  <!-- flash windows -->
  <%= render :partial => 'shared/flash_window' %>
  <div id="header">
    <div class="inner_header clear_wrapper">
      <div class="left">
        <%= link_to image_tag('logo.png', alt:APP_CONFIG['display_app_name'], border: 0, height:'50px'), root_url, class: 'logo' %>
      </div>
      <div class="right">
        <%= render 'users/user_stuff' %>
      </div>
    </div>
  </div>

  <div id="site">
    <%= yield %>
  </div>
    
<%= render 'layouts/bottom' %>
